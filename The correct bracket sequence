def valid_brackets?(str)  #Checking the correct sequence of brackets in a string!
  counter = 0

  str.each_char do |char|
    if char == '('
      counter += 1
    elsif char == ')'
      counter -= 1
    end
    
    return false if counter < 0
  end

  counter == 0
end
